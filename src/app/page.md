## HomePage

**Purpose:** Root `/` route placeholder for Stage 0. Confirms the App Router boots; replaced in Stage 5 by the public landing page.
**File:** `src/app/page.tsx`

### Renders
Static `<main>` with a heading and a Stage 0 placeholder paragraph.

### Exports
- Default: `HomePage`
