## smoke.test.ts

**Purpose:** Trivial Vitest assertion so `pnpm test` has work to do until real tests land in later stages.
**File:** `tests/smoke.test.ts`

### Asserts
- `1 + 1 === 2`. Delete once Stage 2+ produces real test files.
