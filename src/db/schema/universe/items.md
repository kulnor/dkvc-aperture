## items.ts

**Purpose:** Drizzle tables for the EVE item registry hierarchy: category → group → type.
**File:** `src/db/schema/universe/items.ts`

Exports: `universeCategory`, `universeGroup`, `universeType`. See `src/db/schema.md` for columns/FKs. `universeType` is the wormhole-type registry consumed by dogma decoding.
